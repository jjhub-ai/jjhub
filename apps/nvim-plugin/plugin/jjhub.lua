-- Autoload entry point for the JJHub neovim plugin.
-- This file is sourced by Neovim automatically when the plugin is installed.
-- It does NOT call setup() — users must call require("jjhub").setup() in their config.

if vim.g.loaded_jjhub then
  return
end
vim.g.loaded_jjhub = true
