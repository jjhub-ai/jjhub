{
  "name": "@jjhub/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --minify src/index.ts --outfile dist/jjhub",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@jjhub/sdk": "workspace:*",
    "@jjhub-ai/workflow": "workspace:*",
    "hono": "latest",
    "ssh2": "latest"
  },
  "devDependencies": {
    "@types/ssh2": "latest"
  }
}
